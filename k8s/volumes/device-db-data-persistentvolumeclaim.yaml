apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: device-db-data
  name: device-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
